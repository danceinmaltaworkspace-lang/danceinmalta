/**
 * Configurazione contenuti sito (video hero, logo, ecc.)
 * heroVideoId = public_id del video su Cloudinary (come in Media Library). Se il video è in una cartella, usa "cartella/public_id".
 * heroVideoUrl = URL embed esterno (YouTube/Vimeo). Se heroVideoId è vuoto, viene usato questo.
 */
export const siteConfig = {
  heroVideoId: 'VIDEO-2026-02-23-11-23-57_vahtwn',
  heroVideoUrl: '',
}
